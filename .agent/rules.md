# Persistent Agent Rules

1. **Build Process**: Do NOT run `npm run build` after every task. Only run it when the user explicitly asks to push or when performing a final verification before push.
2. **Code Review**: Always perform a code review session following `.agent/workflows/code_review.md` before ending a development session.
3. **Patience**: Wait for user instructions before pushing changes to remote.
